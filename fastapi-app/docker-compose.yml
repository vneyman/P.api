version: "3.9"

services:
  api:
    build: .
    ports:
      - "3020:8000"
    env_file:
      - .env
    volumes:
      - ./app:/app/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    networks:
      - neyman-net

networks:
  neyman-net:
    external: true
